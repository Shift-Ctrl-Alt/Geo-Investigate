<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.oymn.geoinvestigate.dao.mapper.UserRoleDao">
    <insert id="addUserRole">
        insert into
            t_user_role(
            userId,
            roleId,
            create_time
        )values(
                   #{userId},
                   #{roleId},
                   #{createTime}
               )
    </insert>

    <select id="getUserRoleByUserId" resultType="com.oymn.geoinvestigate.dao.pojo.UserRole">
        select
            ur.*,
            r.name roleName,
            r.code roleCode
        from
            t_user_role ur
                left join t_role r on ur.roleId = r.id
        where
            ur.userId = #{userId}
    </select>
</mapper>
